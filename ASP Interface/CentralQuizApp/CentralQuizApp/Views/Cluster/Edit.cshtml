@model CentralQuizApp.Models.Clusters

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>Clusters</legend>

        @Html.HiddenFor(model => model.cluster_id)

        <div class="editor-label">
            @Html.LabelFor(model => model.cluster_title)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.cluster_title)
            @Html.ValidationMessageFor(model => model.cluster_title)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
